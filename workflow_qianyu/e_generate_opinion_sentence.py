def generate_opinion_sentence(
        system_prompt: str,
        interpretation: str,
        first_sentence: str,
        fact_paragraph: str) -> str:
    # return

    # 使用示例
    if __name__ == "__main__":
        generate_opinion_sentence("system_prompt", "interpretation", "first_sentence", "fact_paragraph")